export default function DashboardLayout({ children }: { children: React.ReactNode }) {
  // This file is intentionally minimal so that app/patient/layout.tsx provides the shared sidebar.
  return children
}